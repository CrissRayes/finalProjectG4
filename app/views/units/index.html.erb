<p style="color: green"><%= notice %></p>

<h1>Units</h1>

<div id="units">
  <% @units.each do |unit| %>
    <%= render unit %>
    <p>
      <%= link_to "Show this unit", unit %>
    </p>
  <% end %>
</div>

<% if can? :create, new_unit_path %>
<%= link_to "New unit", new_unit_path %>
<% end %>

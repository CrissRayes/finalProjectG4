<%= form_with(model: unit) do |form| %>
  <% if unit.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(unit.errors.count, "error") %> prohibited this unit from being saved:</h2>

      <ul>
        <% unit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mt-3">
    <%= form.label :name, "Nombre",  style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div class="mt-3">
    <%= form.label :unit_type,"Tipo de Unidad",  style: "display: block" %>
    <%= form.select :unit_type, Unit.unit_types.keys %>
  </div>

  <div class="mt-3">
    <%= form.label :alicuota, "Alícuota",  style: "display: block" %>
    <%= form.text_field :alicuota %>
  </div>

  <div class="mt-3">
    <%= form.submit "Guardar" %>
  </div>
<% end %>
